#include <bits/stdc++.h>
using namespace std;
class queue {
public:
int data;
int *arr;
queue(){
//coding ninja me kiya gya h 
}
};
int main()
{
    /*
    5 types of queue:- 
    1) normal queue
    2) circular queue
    3) input restricted queue - enque only through rear but deque by both side by front and rear both
    4) output restricted queue - deque only through front but enque by both side by front and rear both
    5) Doubly Ended queue(DEque) - we can push and pop through both side rear and front. we can implement stack and queue both by this.
    */
    return 0;
};
